<!DOCTYPE html>
<html lang="en" class="bg-neutral-950 text-gray-100 font-Figtree">
<head>
  <meta charset="UTF-8">
  <title><%= name %> - 2FA Verification</title>
  <link rel="stylesheet" href="/assets/tailwind.css">
  <link href="https://fonts.googleapis.com/css2?family=Figtree:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Figtree', sans-serif; }
    input, button { border-radius: 0.25rem; }
  </style>
</head>
<body class="min-h-screen flex">

<!-- Left: 2FA Form -->
<div class="w-full md:w-1/3 flex flex-col justify-center px-12 
            bg-gradient-to-b from-neutral-900 via-neutral-950 to-neutral-900
            backdrop-blur-md border-r border-neutral-800">
  <h1 class="text-4xl font-bold mb-8"><%= name %> - 2FA Verification</h1>

  <% if (typeof error !== 'undefined') { %>
    <p class="text-red-500 mb-4"><%= error %></p>
  <% } %>

  <form id="twoFAForm" method="POST" action="/login/2fa" class="flex flex-col space-y-4">
    <input type="text" name="token" placeholder="Enter 6-digit code" 
      class="w-full p-3 bg-neutral-900/50 text-gray-100 border border-gray-700 rounded-3xl focus:outline-none focus:ring-2 focus:ring-blue-600" required/>

    <button type="submit" class="py-3 rounded-3xl bg-blue-600 hover:bg-blue-700 font-bold">Verify</button>
  </form>

  <p class="mt-6 text-center text-gray-300">
    <a href="/login" class="text-blue-500 hover:underline font-semibold">Back to Login</a>
  </p>
</div>

<!-- Right: Landscape -->
<div class="hidden md:block w-2/3 relative">
  <img 
    src="https://images.pexels.com/photos/346529/pexels-photo-346529.jpeg" 
    alt="Landscape" 
    class="w-full h-full object-cover brightness-75"
  />
  <div class="absolute inset-0 bg-neutral-950/40 backdrop-blur-sm"></div>
  <div class="absolute inset-0 flex items-center justify-center">
    <h2 class="text-4xl font-bold text-gray-100">Two-Factor Authentication</h2>
  </div>
</div>

</body>
</html>
