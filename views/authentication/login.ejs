<!DOCTYPE html>
<html lang="en" class="bg-neutral-950 text-gray-100 font-Figtree">
<head>
  <meta charset="UTF-8">
  <title><%= name %> - Login</title>
  <link rel="stylesheet" href="/assets/tailwind.css">
  <link rel="icon" type="image/x-icon" href="<%= config.logo %>">
  <link href="https://fonts.googleapis.com/css2?family=Figtree:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Figtree', sans-serif; }
    input, button { border-radius: 0.25rem; }
  </style>
</head>
<body class="min-h-screen flex">

  <!-- Left: Login Form -->
  <div class="w-full md:w-1/3 flex flex-col justify-center px-12 bg-gradient-to-b from-neutral-900 via-neutral-950 to-neutral-900 border-r border-neutral-800">
    <h1 class="text-4xl font-bold mb-8"><%= name %> - Login</h1>

    <% if (typeof error !== 'undefined') { %>
      <p class="text-red-500 mb-4"><%= error %></p>
    <% } %>

    <form id="loginForm" method="POST" action="/login" class="space-y-4">
      <div>
        <label class="block text-xs font-medium text-neutral-600 mb-1.5">
          Email <span class="text-red-600">*</span>
        </label>
        <input
          type="email"
          name="email"
          placeholder="guy@email.com"
          required
          class="w-full px-3 py-2 text-xs focus:outline-none focus:ring-2 focus:ring-neutral-900 bg-neutral-900/60 text-neutral-100 border-neutral-800/60 placeholder-neutral-500"
        />
      </div>

      <div>
        <label class="block text-xs font-medium text-neutral-600 mb-1.5">
          Password <span class="text-red-600">*</span>
        </label>
        <input
          type="password"
          name="password"
          placeholder="Password"
          required
          class="w-full px-3 py-2 text-xs focus:outline-none focus:ring-2 focus:ring-neutral-900 bg-neutral-900/60 text-neutral-100 border-neutral-800/60 placeholder-neutral-500"
        />
      </div>

      <button
        type="submit"
        class="w-full px-3 py-1.5 text-xs font-semibold bg-black dark:bg-white text-white dark:text-black rounded-lg hover:bg-neutral-800 dark:hover:bg-neutral-200 transition"
      >
        Login
      </button>
    </form>

    <p class="mt-6 text-center text-gray-300">
      Don't have an account? 
      <a href="/register" class="text-blue-500 hover:underline font-semibold">Register</a>
    </p>
  </div>

  <!-- Right: Landscape -->
  <div class="hidden md:block w-2/3 relative">
    <img 
      src="https://images.pexels.com/photos/346529/pexels-photo-346529.jpeg" 
      alt="Landscape" 
      class="w-full h-full object-cover brightness-75"
    />
    <div class="absolute inset-0 bg-neutral-950/40 backdrop-blur-sm"></div>
    <div class="absolute inset-0 flex items-center justify-center">
      <h2 class="text-4xl font-bold text-gray-100">Welcome Back</h2>
    </div>
  </div>

</body>
</html>
